import React from 'react';
import PropTypes from 'prop-types';

const Logo = (props) => {
	return(
		<div className='logo'>
		</div>
	);
}

Logo.PropTypes = {
	logo: PropTypes.string.isRequired
}

export default Logo;